spring:
  application:
    name: kafka-consumer-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/
      database: kafka-check
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      ack-mode: record
      concurrency: 3
      type: single
      missing-topics-fatal: false
    consumer:
      group-id: demo-group
      auto-offset-reset: earliest
      enable-auto-commit: false
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      # Read as String first; JSON mapping is done safely in listener code to avoid
      # hard deserialization crashes for plain/base64/invalid payloads.
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      properties:
        spring.deserializer.value.delegate.class: org.apache.kafka.common.serialization.StringDeserializer
        # Heartbeat/session tuning for local cluster resilience.
        session.timeout.ms: 30000
        heartbeat.interval.ms: 10000
        max.poll.interval.ms: 300000
        max.poll.records: 100
        request.timeout.ms: 40000
        default.api.timeout.ms: 45000
        partition.assignment.strategy: org.apache.kafka.clients.consumer.CooperativeStickyAssignor
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      # JSON serializer used by app producer and DLT publisher.
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false

app:
  kafka:
    topic: demo-topic
    dlt-topic: demo-topic-dlt
    dlt-group-id: demo-group-dlt
    retry-attempts: 5
    retry-interval-ms: 1000
    max-retry-interval-ms: 10000
    non-retryable-exceptions:
      - java.lang.IllegalArgumentException
      - com.fasterxml.jackson.core.JsonProcessingException
    session-timeout-ms: 30000
    heartbeat-interval-ms: 10000
    max-poll-interval-ms: 300000
    max-poll-records: 100
  mongodb:
    consumer-collection: kafka_log
    dlt-collection: kafka_dlt_log

logging:
  level:
    root: INFO
    com.example.consumer: INFO
    org.springframework.kafka: INFO
    org.apache.kafka.clients.consumer.internals: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n"
